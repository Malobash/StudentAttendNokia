{
  "name": "student-attend-nokia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm start --workspace=backend\"",
    "install-all": "npm install --workspaces"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
